import styled, { keyframes } from "styled-components";

const Contact = () => {
  return <h1>This is the estimate request page! </h1>;
};

export default Contact;

const Div = styled.div`
  height: 800px;
  border: 1px solid red;
`;
